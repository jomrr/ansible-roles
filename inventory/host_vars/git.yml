---
# role: jomrr.git
# file: inventory/host_vars/git.yml

# default meta data
meta_year_created: "2024"

# default readme settings
read_description: |-
  This Ansible role installs and configures git on supported platforms.

# role default variables from meta/argument_specs.yml
read_variables: []

# Example playbooks
read_examples:
  - name: Simple example playbook
    description: |-
      A simple default example playbook for using jomrr.git.
    code: |-
      ---
      # name: "jomrr.git"
      # file: "playbook_git.yml"

      - name: "PLAYBOOK | git"
        hosts: "git"
        gather_facts: true
        roles:
          - role: "jomrr.git"

# references as single line in markdown
read_references: []
