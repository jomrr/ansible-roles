---
# role: jomrr.postgresql
# file: inventory/host_vars/postgresql.yml

# default meta data
meta_year_created: "2019"

# default readme settings
read_description: |-
  This Ansible role installs and configures postgresql on supported platforms.

# role default variables from meta/argument_specs.yml
read_variables: []

# Example playbooks
read_examples:
  - name: Simple example playbook
    description: |-
      A simple default example playbook for using jomrr.postgresql.
    code: |-
      ---
      # name: "jomrr.postgresql"
      # file: "playbook_postgresql.yml"

      - name: "PLAYBOOK | postgresql"
        hosts: "postgresql"
        gather_facts: true
        roles:
          - role: "jomrr.postgresql"

# references as single line in markdown
read_references: []
