---
# role: jomrr.qemu
# file: inventory/host_vars/qemu.yml

# default meta data
meta_year_created: "2019"

# default readme settings
read_description: |-
  This Ansible role installs and configures qemu on supported platforms.

# role default variables from meta/argument_specs.yml
read_variables: []

# Example playbooks
read_examples:
  - name: Simple example playbook
    description: |-
      A simple default example playbook for using jomrr.qemu.
    code: |-
      ---
      # name: "jomrr.qemu"
      # file: "playbook_qemu.yml"

      - name: "PLAYBOOK | qemu"
        hosts: "qemu"
        gather_facts: true
        roles:
          - role: "jomrr.qemu"

# references as single line in markdown
read_references: []
