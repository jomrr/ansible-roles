---
# name: {{ meta_namespace | default('jam82') }}.{{ meta_repo_name }}
# file: molecule/resources/playbooks/converge.yml

{% if mole_converge | default([]) | length > 0 -%}
{{ mole_converge | jam82.dev.to_lintable_yaml(indent=2) }}
{% else -%}
{% raw -%}
- name: "PLAYBOOK | Converge"
  hosts: all
  roles:
    - role: "{{ lookup('ansible.builtin.env', 'MOLECULE_PROJECT_DIRECTORY') }}"
{% endraw %}
{% endif %}
