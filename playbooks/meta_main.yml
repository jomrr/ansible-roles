---
# name: ansible-roles
# file: playbooks/meta_main.yml

- name: "Playbook 'meta_main' for role '{{ meta_role_name }}"
  hosts: "{{ target | default('all') }}"
  become: false
  gather_facts: false
  tasks:
    - name: "Ensure that path exists"
      ansible.builtin.file:
        path: "{{ ansible_host }}/meta"
        state: directory
        mode: "0755"

    - name: "Generate meta/main.yml"
      ansible.builtin.template:
        src: "meta/main.yml.j2"
        dest: "{{ ansible_host }}/meta/main.yml"
        mode: "0644"
        validate: "ansible-lint %s"
